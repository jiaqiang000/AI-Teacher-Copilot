# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.coverage
.coverage.*
.ruff_cache
agent_history.gif
static/browser_history/*.gif

log/
log/*

# Virtual environments
.venv
venv/

# User config file
config.yaml
# 例外：teacher-copilot 的 agent 定义（不含密钥，需纳入版本控制）
!.deer-flow/agents/teacher-copilot/config.yaml

# Langgraph
.langgraph_api

# Sandbox runtime working dir — pre-created and excluded from uvicorn reload
# (scripts/serve.sh, docker/dev-entrypoint.sh). Anchored so it does not match
# the source package backend/packages/harness/deerflow/sandbox/.
/sandbox/

# Claude Code settings
.claude/settings.local.json

# pytest --basetemp workaround dirs (sandbox tmp_path permission)
.pytest_tmp/
.pytest_tmp_run/

# Teacher Copilot 真实密钥(勿提交)
.env
tc-data/
