# 演示静默登录（2026-09-05）

## 重启验证补充

已将学生作业页状态查找改为 `STATUS_META[status ?? ""] ?? 默认标签`，避免空字符串进入标签对象；生产构建及类型检查通过，前后端已重启。
此前教师演示被新增的管理员检查拦截。用户明确要求与原有体验账号的普通登录一致，现已移除额外限制，直接复用 `login_local`，不改账号权限。

## 需求与实现

- 依据 Figma `0ugwazaOoaDw71Nlrak6tB / 76:3`，保留上方邮箱密码表单，下方放教师/学生演示卡片；窄屏单列。
- 本轮范围由设计稿同步扩展为前后端实现。前端只发 role、remember_me，后端使用原页面上对应体验账号的邮箱密码调用普通登录，不在表单显示凭据。
- 成功整页进入 `/workspace`，现有角色分流送教师到 `/workspace/teacher-copilot/dashboard`、学生到 `/workspace/teacher-copilot/student/homework`。演示不采用 next，避免跨角色跳转。
- `/` 未登录和 `/welcome` 都进入 `/login`，删除独立引导页面内容（Git 可恢复）。
- 请求期间禁用登录/演示按钮；失败显示提示并可重试，不改变邮箱密码和本地记忆偏好。

## 运维边界

共享体验账号保留已有权限（当前教师账号具有管理员权限），并非只读沙箱。
登录不会创建账号、重置密码或修改业务映射；密码验证、错误响应与普通登录保持一致，不新增管理员或业务映射准入限制。

部署需重启 Gateway 并重新构建生产前端；本地正在运行的旧生产进程不会自动读取此改动。

## 验证

- 后端：移除多余限制后，`tests/test_auth.py tests/test_auth_type_system.py tests/test_demo_login.py`，148 项通过。
- 前端：`tests/e2e-auth/demo-login.spec.ts` 的 5 项浏览器测试通过，覆盖两身份请求、表单不变、失败重试、桌面/移动布局、旧入口重定向；使用网络拦截，不冒充真实全链路验收。桌面/移动端截图已人工检查。
- 全量类型检查受已有学生作业页 `page.tsx:88,90` 的空字符串联合类型错误影响；未修改该用户已有改动。
- 全量 `pnpm check` 另有 118 项既有 ESLint 错误；本次修改文件的定向 ESLint、后端定向 Ruff 和 `git diff --check` 通过。
